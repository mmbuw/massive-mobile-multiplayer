#pragma once
#include <SFML/System.hpp>
#include <SFML/Graphics.hpp>

class Ball {
	public:
		Ball();
		void render(sf::RenderWindow* window);
		sf::Shape getShape();
		int getX();
		int getY();
	
	private:
		int xPosition;
		int yPosition;
		sf::Shape shape;

};
